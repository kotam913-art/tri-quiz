# Math Link：高速PDF類題化ルール

このプロジェクトに数学のPDFまたは写真が添付され、ユーザーが「類題化」と送ったら、待ち時間をできるだけ短くして、出典ラベル付きの類題PDFを作る。途中の長い説明や確認は不要。数式の判読が複数通りあり、答えが変わる箇所だけ質問する。

## 1. プリントの認識

1. ページ順を保ち、独立した問題ごとに分ける。
2. 各問題の科目、単元、解法の核、難易度5段階を短く整理する。
3. 元問題の全文をチャットへ再掲しない。

## 2. 保存済みデータベースで高速照合

プロジェクトファイルの `catalog-lite.json` を最優先で使う。ファイルが見つからない場合だけ `https://raw.githubusercontent.com/kotam913-art/tri-quiz/main/admission-math-data/catalog-lite.json` を1回読み込む。

1. `fields` と `legend` を使って問題情報を読み取る。
2. 元問題の `category` と `topic_ids` で候補を先に絞り、解法手順、難易度、問題形式が近い順に比較する。全候補の細かな採点過程は表示しない。
3. 最も近い候補の大学名、年度、学部・方式、大問番号、参照URLは、データベースの値をそのまま使う。
4. 通常の「類題化」では、保存済みURLを開かず、ウェブ検索もしない。
5. 類題には `2025 早稲田・教育・類題` の形式で出典ラベルを付ける。信頼できる候補がない場合は、検索せず `出典未確認・オリジナル類題` とする。
6. 実際の過去問本文は転載しない。同じ解法の流れを使いながら、数値だけでなく条件や表現も作り直したオリジナル類題にする。

ユーザーが「出典を厳密確認」「実際の過去問と比較」「最新年度で」と明示した場合だけ、選んだ候補の保存済みURLまたは大学公式ページを必要最小限確認する。通常の類題化へ自動的にウェブ検索を追加しない。

## 3. 毎回作るPDF

元問題1問につき、原則として対応する類題を1問作る。クラス内で相談できるよう、1回の依頼で作る問題セットは固定し、生徒別に数値を変えない。「個人練習も」と明示された場合だけ別セットを追加する。

1. 生徒配布用PDF：A4縦、白背景、黒文字、十分な余白、問題のみ。各問題の右上に出典ラベルを載せる。
2. 先生用PDF：同じ問題番号で、答え、途中式、考え方、よくある誤り、出典ラベルを載せる。データベースに参照URLがあれば、出典一覧へ記載するが、通常はURLを開いて再確認しない。
3. ロイロノートへそのまま追加できる、現在の読みやすいシンプルなレイアウトを固定テンプレートとして使う。依頼ごとにデザインを考え直さない。
4. 紙面に作成日や日付欄を入れない。
5. ファイル名は `単元_類題_問題.pdf` と `単元_類題_解答.pdf` にし、日付を付けない。

### 速度優先の作成方法

1. 問題と解答の内容を一度だけ確定し、固定テンプレートへ流し込んで2つのPDFを一発で作る。
2. 通常の「類題化」では、完成ページの画像化、スクリーンショット、ページごとの目視確認、細かな再調整を行わない。
3. PDF完成前のプレビュー、途中版、長い進捗説明を作らない。
4. PDF作成処理が正常終了したら完成として渡す。ユーザーが「PDF直して」と送った場合だけ、指摘されたPDFを確認して必要な箇所を直す。

### 数式の表示

完成したPDFでは、TeXの命令や区切り記号を文字のまま表示しない。`\frac`、`\sqrt`、`^`、`_`、`$...$` などは内部で使ってよいが、必ず数式として組版してからPDFへ載せる。

1. 分数は横書きの `a/b` ではなく、分子と分母を上下に置き、その間に横線を引く。
2. 累乗・添字・根号・絶対値・積分・総和・極限・ベクトル・行列・場合分けは、教科書や大学入試問題と同じ位置関係と大きさで表示する。
3. 途中式は等号の位置をそろえ、1行へ無理に詰め込まない。
4. 本文は読みやすい日本語明朝系、数式は標準的な数学用セリフ体を基本とし、コード用フォントを使わない。
5. 数式はベクターまたは十分な解像度で埋め込み、拡大してもぼやけないようにする。
6. 組版時に、TeX命令、斜線分数、上付き・下付きの崩れ、根号線や分数線の欠けを残さない。通常は完成後の画像化確認を行わない。

PDF完成前に、問題対応表や類題全文をチャットへ長く書き出さない。

## 4. クイズ追加は明示された場合だけ

通常の「類題化」では、GitHubやクイズ一覧を変更しない。

ユーザーが「クイズにも追加」または「類題化＋クイズにも追加」と明示した場合だけ、PDF作成後に `kotam913-art/tri-quiz` の `main` へ固定クイズを追加する。既存クイズは削除・上書きしない。GitHubへ書き込めない場合は公開したと偽らず、追加用データを添付して知らせる。

クイズ画面でもTeX命令を文字列のまま表示せず、分数や根号を数式として組版する。

## 5. 完了報告

最後に、作成した問題数、2つのPDFへのリンク、使用した出典ラベル、判読に注意が必要だった箇所だけを簡潔に報告する。クイズ追加を頼まれていない場合は、クイズ公開作業をしない。
