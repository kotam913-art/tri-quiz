# Math Link：自作プリント類題化ルール

このプロジェクトに数学のPDFまたは写真が添付され、ユーザーが「類題化」と送ったら、以下を一連の作業として実行する。余計な確認はせず進める。ただし、数式の判読が複数通りあり答えが変わる箇所だけは、該当箇所を示して確認する。

## 1. プリントの解析

1. ページ順を保ち、独立した問題ごとに分割する。
2. 各問題について、科目、分野、単元、解法の核、難易度5段階、想定大学群を整理する。
3. 元問題の全文を回答本文に再掲せず、問題番号と短い要約で対応づける。

## 2. データベースを使った出典検索と類題

毎回ウェブ全体を検索してはいけない。プロジェクトファイルの `catalog.json` を最優先で使う。プロジェクトに見つからない場合だけ、`https://raw.githubusercontent.com/kotam913-art/tri-quiz/main/admission-math-data/catalog.json` を直接読み込む。

1. 元問題から、科目、主分野、副分野、必要な概念、解法手順、表現形式、解答形式、場合分けや図の有無、難易度5成分を抽象化する。具体的な式や数値は照合キーにしすぎない。
2. `catalog.json` の `indexes` で候補を絞り、各レコードの `problem_features` を次の点数で照合する。
   - `topic_ids` が1つ以上一致：+5
   - `category` が一致：+4
   - `concepts` の一致：1項目につき+1、最大+4
   - `solution_steps` の思考順序が同じ：+4
   - `representations` と `structure_flags` が近い：最大+3
   - `response_mode` が一致：+1
   - `difficulty_profile.overall` の差が0.5以内：+2、1以内：+1
   - 希望大学群と `group` が一致：+2
   - `confidence.level` が `low`：-2
3. 上位3件を比較し、10点以上で矛盾がない候補を主出典にする。この条件を満たす候補があれば、保存済みURLを開くこともウェブ検索も行わない。
4. 大学名、年度、学部・方式、大問番号、参照URLは、選んだレコードの値をそのまま使う。確認できない情報を推測で補わない。
5. 実際の過去問本文は転載せず、データベースの出題情報を参考に新しいオリジナル類題を作る。
6. 類題には必ず `2025 早稲田・教育・類題` の形式でラベルを付ける。大学・年度・学部を確認できない場合は大学名を付けず、`出典未確認・オリジナル類題` とする。
7. 数値だけを変えた問題にせず、元問題と同じ思考手順を使いながら表現や条件を作り直す。

保存済みURLを開くのは、最高候補が10点未満、`confidence.level` が `low`、出典URLの提示を厳密に再確認する必要がある、またはユーザーが実際の過去問内容との比較を明示した場合だけとする。ウェブ全体を検索するのは、データベースに同分野の候補がない、保存済みURLが開けない、またはユーザーが最新年度を明示した場合だけとする。その場合も広く検索せず、大学公式または信頼できる過去問提供元を1回確認する。確認できた新規情報は、次回から再検索しなくてよいよう追加候補として整理する。

## 3. 出力物

毎回、次をまとめて作る。

1. 問題対応表：元問題番号、分野、難易度、近い過去問情報、作成した類題番号。
2. 生徒配布用PDF：A4縦、白背景、黒文字、十分な余白、問題のみ。各問題の右上に出典ラベルを表示する。
3. 先生用PDF：同じ問題番号で、答え、途中式、考え方、よくある誤りを掲載する。
4. PDFはiPadのロイロノートへそのまま教材として追加できるよう、1ページを詰め込みすぎず、数式や図が切れないことを画像化して確認する。
5. ファイル名は `YYYYMMDD_単元_類題_問題.pdf` と `YYYYMMDD_単元_類題_解答.pdf` にする。

クラスで相談できるよう、1回の依頼で作る問題セットは固定する。生徒別の数値変更は行わない。「個人練習も」と明示された場合だけ、別セットとして追加する。

## 4. クイズへの追加

ユーザーが「クイズにも追加」と送った場合は、PDF作成に加えて `kotam913-art/tri-quiz` の `main` に固定クイズを追加する。

1. `generated-quiz-data/<id>.json` を新規作成する。`id` は半角英数字とハイフンだけで、日付と単元が分かる名前にする。
2. `generated-quiz-data/index.json` の `quizzes` 先頭へ同じセットの一覧情報を追加する。
3. 既存クイズや既存JSONを削除・上書きしない。
4. 公開後、クイズ一覧にカードが現れ、問題・答え・解説・出典リンクが開けることを確認する。
5. GitHubへの書き込み機能が利用できない場合は、公開したと偽らず、追加用JSONファイルを添付してユーザーへ知らせる。

### 一覧JSON

```json
{
  "id": "2026-08-14-quadratic-functions",
  "title": "二次関数 類題セット",
  "summary": "自作プリントに対応した固定類題です。",
  "unit": "二次関数",
  "level": "MARCH〜早慶",
  "estimated_minutes": 15
}
```

### 問題JSON

```json
{
  "schema_version": 1,
  "id": "2026-08-14-quadratic-functions",
  "title": "二次関数 類題セット",
  "summary": "自作プリントに対応した固定類題です。",
  "subject": "数学I",
  "unit": "二次関数",
  "level": "MARCH〜早慶",
  "estimated_minutes": 15,
  "source_label": "2025 早稲田・教育・類題",
  "pdf_url": "",
  "questions": [
    {
      "number": 1,
      "prompt": "問題文",
      "answer": "答え",
      "explanation": "途中式を含む解説",
      "source_label": "2025 早稲田・教育・類題",
      "source_url": "確認済みの参照URL"
    }
  ]
}
```

## 5. 完了報告

最後に、作成した問題数、PDFへのリンク、クイズ公開URL、使用した出典一覧、判読に注意が必要だった箇所を簡潔に報告する。クイズ追加を頼まれていない場合は、クイズ公開URLを作らない。
