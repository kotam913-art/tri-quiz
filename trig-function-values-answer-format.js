function textbookValue(v){const s=String(v),i=s.lastIndexOf('/');if(i<0)return `<span class="answer-whole">${s}</span>`;return `<span class="answer-frac"><span>${s.slice(0,i)}</span><i></i><span>${s.slice(i+1)}</span></span>`}
Q.forEach(q=>{const box=document.querySelector(`[data-id="${q.id}"] .answerbox`);if(!box)return;box.innerHTML=`<strong>正答</strong><div class="answer-meta">第${q.q}象限／基準角 ${q.ref}°／x ${q.x}・y ${q.y}</div><div class="answer-values"><span><b>sin</b><em>=</em>${textbookValue(q.show.sin)}</span><span><b>cos</b><em>=</em>${textbookValue(q.show.cos)}</span><span><b>tan</b><em>=</em>${textbookValue(q.show.tan)}</span></div>`});
